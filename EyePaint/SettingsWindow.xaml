<Window x:Class="EyePaint.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Topmost="True">
    <StackPanel Margin="10">
        <Label Content="Epost:"/>
        <TextBox Text="{Binding AdminEmail}" />
        <Label Content="SMTP server:"/>
        <TextBox Text="{Binding SmtpServer}" />
        <Label Content="Flickr (key):"/>
        <TextBox Text="{Binding FlickrKey}" />
        <Label Content="Flickr (secret):"/>
        <TextBox Text="{Binding FlickrSecret}" />
        <Button Margin="0,10,0,0" Name="SaveButton" Click="onSaveButtonClick" Content="Spara inställningarna"/>
    </StackPanel>
</Window>